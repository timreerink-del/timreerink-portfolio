---
interface Experiment {
  name: string;
  summary: string;
  status: 'live' | 'beta' | 'building' | 'failed';
  url?: string;
  accent: 'cobalt' | 'lime' | 'blue' | 'amber' | 'pink' | 'violet';
  tags?: string[];
}

interface Props {
  variant?: 'page' | 'section';
}
const { variant = 'section' } = Astro.props;

const experiments: Experiment[] = [
  {
    name: 'Homepage',
    summary: 'A quick little dashboard with my calendar, to-do list, WhatsApp shortcuts, Deezer, docs, links and a rotating set of inspirational quotes. Nothing game-changing — but great for personal organising.',
    status: 'live',
    accent: 'cobalt',
    tags: ['Personal', 'Dashboard'],
  },
  {
    name: 'promptdesign.space',
    summary: 'A curated space for Claude skills, prompts, agents and pipelines. A scraper pulls fresh publications weekly; I curate and post them on LinkedIn and the vault itself.',
    status: 'live',
    url: 'https://promptdesign.space/',
    accent: 'lime',
    tags: ['Prompts', 'Agents', 'Pipelines'],
  },
  {
    name: 'Design Manifesto',
    summary: 'My design philosophy for Randstad, in a nutshell.',
    status: 'live',
    url: 'https://dmp-design-manifesto.netlify.app/',
    accent: 'amber',
    tags: ['Manifesto', 'Principles'],
  },
  {
    name: 'Kintsu',
    summary: 'A next-level journaling app for our next of kin — a legacy you can interact with.',
    status: 'building',
    accent: 'violet',
    tags: ['Journaling', 'Legacy', 'AI'],
  },
  {
    name: 'Vellum',
    summary: 'A subscription-based tattoo platform and marketplace.',
    status: 'building',
    accent: 'pink',
    tags: ['Marketplace', 'Subscription'],
  },
  {
    name: 'Scrapelaar',
    summary: 'A Funda / real-estate scraper that compares listings at the moment they are placed — to check if a buyer can benefit between agent and platform. POC didn’t pan out.',
    status: 'failed',
    accent: 'blue',
    tags: ['Scraper', 'Real estate'],
  },
];

const statusLabel = {
  live: 'Live',
  beta: 'Beta',
  building: 'Building',
  failed: 'POC Failed',
};
---

<section
  id="experiments"
  class:list={['experiments', 'container', { 'section': variant === 'section', 'page': variant === 'page' }]}
  aria-labelledby="experiments-h"
>
  <header class="experiments-head">
    <span class="mono experiments-eyebrow">AI Experiments</span>
    <h2 id="experiments-h" class="experiments-title serif-display">
      Tools I'm building <span class="italic">in public.</span>
    </h2>
    <p class="experiments-lede">
      A scratchpad of small AI tools — some live, some half-baked, all built to test ideas
      about how design and language models actually work together.
    </p>
  </header>

  <ul class="experiments-grid" role="list">
    {experiments.map((e, i) => {
      const num = String(i + 1).padStart(2, '0');
      const isLink = e.url && e.url !== '#';
      const Tag = isLink ? 'a' : 'div';
      return (
        <li class="experiment-cell">
          <Tag
            href={isLink ? e.url : undefined}
            target={isLink ? '_blank' : undefined}
            rel={isLink ? 'noopener' : undefined}
            class:list={['experiment', { 'is-link': isLink, 'is-failed': e.status === 'failed' }]}
            style={`--case-color: var(--c-${e.accent});`}
          >
            <header class="experiment-head mono">
              <span class="experiment-num">EXP · {num}</span>
              <span class:list={['experiment-status', e.status]}>
                <span class="status-dot" aria-hidden="true"></span>
                {statusLabel[e.status]}
              </span>
            </header>

            <h3 class="experiment-name serif-display">
              <span class="italic">{e.name}</span>
            </h3>

            <p class="experiment-summary">{e.summary}</p>

            {e.tags && e.tags.length > 0 && (
              <ul class="experiment-tags mono" role="list">
                {e.tags.map(t => <li>{t}</li>)}
              </ul>
            )}

            {isLink && (
              <span class="experiment-cta mono">
                Open ↗
              </span>
            )}
          </Tag>
        </li>
      );
    })}
  </ul>
</section>

<style>
  .experiments.section { padding-block: var(--section-y); }
  .experiments.page {
    padding-top: clamp(140px, 18vh, 220px);
    padding-bottom: var(--section-y);
  }

  .experiments-head {
    display: grid;
    gap: 16px;
    margin-bottom: clamp(40px, 6vw, 64px);
    max-width: 800px;
  }
  .experiments-eyebrow { color: var(--muted); }
  .experiments-title {
    font-size: var(--t-h1);
    font-weight: 300;
    margin: 0;
    color: var(--fg);
    max-width: 14ch;
  }
  .experiments-title .italic {
    color: var(--c-cobalt);
    font-weight: 400;
    font-variation-settings: "opsz" 144, "SOFT" 50;
  }
  .experiments-lede {
    margin: 0;
    color: var(--muted);
    font-size: 17px;
    line-height: 1.55;
    max-width: 56ch;
  }

  .experiments-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  .experiment-cell { display: flex; }

  .experiment {
    flex: 1;
    display: grid;
    grid-template-rows: auto auto auto auto auto;
    gap: 16px;
    padding: 24px;
    border: 1px solid var(--line);
    border-radius: 14px;
    background: var(--bg-2);
    color: var(--fg);
    transition: border-color 240ms ease, transform 240ms ease, background 240ms ease;
    isolation: isolate;
    position: relative;
    overflow: hidden;
  }
  .experiment::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--case-color);
    opacity: 0;
    transition: opacity 240ms ease;
    pointer-events: none;
    mix-blend-mode: soft-light;
  }
  .experiment.is-link:hover {
    border-color: var(--case-color);
    transform: translateY(-2px);
  }
  .experiment.is-link:hover::before { opacity: 0.18; }
  .experiment.is-link:hover .experiment-cta { color: var(--case-color); }

  .experiment.is-failed { opacity: 0.62; }
  .experiment.is-failed .experiment-name .italic { text-decoration: line-through; text-decoration-thickness: 1.5px; text-decoration-color: color-mix(in oklab, var(--case-color) 55%, transparent); }
  .experiment.is-failed:hover { opacity: 0.95; }

  .experiment-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: var(--muted);
  }
  .experiment-num {
    color: var(--case-color);
    font-weight: 500;
  }

  .experiment-status {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 5px 9px 4px;
    border: 1px solid var(--line);
    border-radius: 999px;
    font-size: 9px;
    line-height: 1;
    background: color-mix(in oklab, var(--bg) 60%, transparent);
    color: var(--muted);
  }
  .experiment-status .status-dot {
    width: 5px;
    height: 5px;
    border-radius: 999px;
    background: var(--muted);
    flex-shrink: 0;
  }
  .experiment-status.live .status-dot     { background: var(--c-lime); }
  .experiment-status.beta .status-dot     { background: var(--c-amber); }
  .experiment-status.building .status-dot { background: var(--c-cobalt); }
  .experiment-status.failed .status-dot   { background: var(--c-pink); }
  .experiment-status.failed { color: var(--c-pink); }

  .experiment-name {
    font-family: var(--serif);
    font-weight: 300;
    font-size: 28px;
    line-height: 1.05;
    margin: 0;
    color: var(--fg);
  }
  .experiment-name .italic {
    font-style: italic;
    color: var(--case-color);
    font-variation-settings: "opsz" 96, "SOFT" 80;
  }

  .experiment-summary {
    margin: 0;
    color: var(--muted);
    font-size: 14px;
    line-height: 1.5;
  }

  .experiment-tags {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }
  .experiment-tags li {
    padding: 5px 9px 4px;
    border: 1px solid var(--line);
    border-radius: 999px;
    color: var(--muted);
    font-size: 9px;
    line-height: 1;
  }

  .experiment-cta {
    align-self: end;
    color: var(--muted);
    transition: color 200ms ease;
    padding-top: 8px;
    border-top: 1px solid var(--line);
  }

  @media (max-width: 1024px) {
    .experiments-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 560px) {
    .experiments-grid { grid-template-columns: 1fr; }
  }
</style>
